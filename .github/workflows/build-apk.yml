name: Build Luna VPN APK

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Check directory structure
      run: |
        echo "=== Root directory ==="
        ls -la
        echo ""
        echo "=== v2rayNG directory ==="
        ls -la v2rayNG/ || echo "v2rayNG not found"
        echo ""
        echo "=== v2rayNG/V2rayNG directory ==="
        ls -la v2rayNG/V2rayNG/ || echo "v2rayNG/V2rayNG not found"
        echo ""
        echo "=== Looking for gradlew ==="
        find . -name "gradlew" -type f || echo "gradlew not found"

    - name: Grant execute permission for gradlew
      run: |
        cd v2rayNG/V2rayNG
        chmod +x gradlew

    - name: Build Debug APK
      run: |
        cd v2rayNG/V2rayNG
        ./gradlew assembleDebug --stacktrace --info

    - name: Build Release APK (unsigned)
      run: |
        cd v2rayNG/V2rayNG
        ./gradlew assembleRelease --stacktrace
      continue-on-error: true

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: luna-vpn-debug
        path: v2rayNG/V2rayNG/app/build/outputs/apk/debug/*.apk
        retention-days: 30

    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: luna-vpn-release
        path: v2rayNG/V2rayNG/app/build/outputs/apk/release/*.apk
        retention-days: 30
      continue-on-error: true

    - name: Get APK info
      run: |
        echo "=== Build completed ==="
        echo "Debug APK:"
        ls -lh v2rayNG/V2rayNG/app/build/outputs/apk/debug/*.apk || echo "No debug APK found"
        echo ""
        echo "Release APK:"
        ls -lh v2rayNG/V2rayNG/app/build/outputs/apk/release/*.apk || echo "No release APK found"
